#!/bin/bash

# commitizen init cz-conventional-changelog --dev --exact

# npm i --save dockerode dockerode-compose detect-indent fs-extra colorette

name=types
mkdir packages/$name
touch packages/$name/.gitkeep
